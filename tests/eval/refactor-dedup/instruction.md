`process.sh`는 레코드 3개를 처리하는 동작하는 스크립트이지만,
동일한 로직 블록이 3번 반복되어 있다.

**태스크**: `process.sh`만 수정하여 중복 블록을 함수로 추출하라.

조건:
- `bash process.sh` 의 출력이 수정 전과 완전히 동일해야 한다
- `process.sh` 파일만 수정한다 (다른 파일 생성·수정 금지)
